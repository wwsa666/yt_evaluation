-- 校园美食雷达 - 第一阶段：分类初始化 SQL
-- 目标表: shop_types
-- 注意：此脚本会清空现有的店铺分类，并插入新的五大分类：美食、奶茶、影院、娱乐、超市。
-- 警告：如果现有的 shops 表中有关联到被删除的 type_id，这会导致孤立数据。
-- 建议在执行此脚本前，确认这是否在全新环境测试，或者请手动将 shops 的 type_id 更新为新的分类。

USE `gradate_design_db`;

-- 清空原分类数据
TRUNCATE TABLE `shop_types`;

-- 插入“校园美食雷达”核心分类
INSERT INTO `shop_types` (`name`, `sort`) VALUES
('美食', 1),
('奶茶', 2),
('影院', 3),
('娱乐', 4),
('超市', 5);

-- (备注：图标暂时使用系统默认头像占位，后续可更换为食物相关图标)
